# Base URL of the DealerPilot backend API (FastAPI). The frontend never
# talks to vAuto directly and never holds vAuto credentials -- only the
# backend does that. Nothing secret belongs in this file.
VITE_API_BASE_URL=http://127.0.0.1:8000
